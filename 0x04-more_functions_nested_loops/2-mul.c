#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiplies two integers
 * @a: is the first integer
 * @b: is the second integer
 * Return: Always 0
 */

int mul(int a, int b)
{
	int mul;

		mul = a * b;

	return (mul);
}
